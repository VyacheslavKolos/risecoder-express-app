const EMAIL_TYPES = {
  WELCOME: 'welcome',
  USER_BLOCKED: 'user_blocked',
};
const COMMON_ERRORS = {
  VALIDATION_ERROR: 'validation_error',
};

module.exports = {
  EMAIL_TYPES,
  COMMON_ERRORS,
};
